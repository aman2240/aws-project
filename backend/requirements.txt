# Pinned to exact versions on purpose — nothing should break from an
# upstream release mid-hackathon. Bump deliberately, not via `-U`.

fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1
amazon-transcribe==0.6.2
slack-sdk==3.33.1
cedarpy==4.12.0
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
opensearch-py==2.7.1
deepgram-sdk==3.7.0
groq==0.11.0
pydantic-settings==2.5.2
pytest==8.3.3
pytest-asyncio==0.24.0

# Not in the original pin list, but required transitively:
# fastapi.testclient.TestClient is httpx-backed since Starlette dropped
# `requests`, and httpx isn't pulled in by fastapi's base install.
httpx==0.27.2
# Slack's interaction webhook posts application/x-www-form-urlencoded;
# Starlette's Request.form() needs this to parse it and isn't pulled
# in by fastapi's base install either.
python-multipart==0.0.20
# opensearch_client.py uses AsyncOpenSearch, whose aiohttp-based
# transport opensearch-py doesn't declare as a hard dependency (only
# `certifi`, `Events`, `python-dateutil`, `requests`, `urllib3` are).
# Was present only transitively via deepgram-sdk — pinned explicitly
# so it doesn't silently disappear if that pin ever changes.
aiohttp==3.14.3
